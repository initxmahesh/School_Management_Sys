import { SectionShell } from "./section-shell";

export function GallerySection() {
  return (
    <SectionShell
      id="gallery"
      title="Gallery"
      description="A glimpse into school life — classrooms, events, sports, and celebrations."
      className="bg-white"
    />
  );
}
