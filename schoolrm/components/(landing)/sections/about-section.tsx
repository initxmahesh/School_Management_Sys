import { SectionShell } from "./section-shell";

export function AboutSection() {
  return (
    <SectionShell
      id="about"
      title="About Us"
      description="Learn about our mission, values, and the community that makes The British School a place where students thrive."
    />
  );
}
